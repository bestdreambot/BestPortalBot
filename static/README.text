
СТАТИЧНЫЙ КОД BestPortalBot 🔑

1. Всегда чёрный экран (#000).
2. Всегда стартовый экран с логотипом (logo).
3. Всегда переключение: клик по логотипу -> экран "Вход в портал".
4. Эти элементы не меняются при обновлениях динамических функций.

Файлы:
- index.html (базовая структура, контейнеры)
- style.css (фон, отступы, экран, портал, debug)
- script.js (переключение экранов, Telegram expand, логика загрузки лого)
